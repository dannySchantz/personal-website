# 1-D-Fission-Reactor-MC cleanup delivery

This folder is a temporary handoff — **do not merge into main**.

## Fastest path (Mac)

```bash
# 1. Clone or open your Monte Carlo repo
cd ~/path/to/1-D-Fission-Reactor-MC

# 2. Download this zip from GitHub, unzip it, then:
bash /path/to/1-D-Fission-Reactor-MC-cleaned/PUSH-TO-GITHUB.sh /path/to/1-D-Fission-Reactor-MC-cleaned
```

Or copy the `1-D-Fission-Reactor-MC-cleaned/` folder contents over your repo (keep `.git`), commit, and push.

## Files

| Path | What it is |
|------|------------|
| `PUSH-1D-MC-CLEANUP.zip` | Zip of the cleaned project + push script |
| `1-D-Fission-Reactor-MC-cleaned/` | Unzipped cleaned project (browse on GitHub) |
